<!-- snippets/component__tooltip.liquid -->
{% comment %} 
  This snippet creates a tooltip that appears on hover. It is styled to match the theme's secondary color scheme and includes a triangular pointer.

  Globals:
  - settings.color_background_secondary: {string} The background color of the tooltip, typically matching the theme's secondary color.

  Accepts:
  - content: {string} The text or HTML content to be displayed inside the tooltip.
  - background: {string} The color scheme to be applied to the tooltip.
  - text: {string} The text color to be applied to the tooltip.
  - tip: {string} The position of the tip. ['left', 'center', 'right']
  - container_class: {string} The class to be applied to the container of the tooltip.

  Usage:
    {% render 'component__tooltip', 
      content: 'Your tooltip content here',
      background: 'text',
      text: 'light',
      tip: 'center',
      container_class: 'mt-2'
    %}

  Recommendations:
  - Use this snippet to provide additional information on hover, enhancing user experience without cluttering the UI.
  - Place this within a container with class 'group' and 'relative'
{% endcomment %}

<div 
  class="
    absolute hidden px-4 py-2 group-hover:flex type--smaller border--radius mt-2 md:max-w-[400px] shadow-md top-full z-10 justify-center w-max
    color__bg-{{ background }} 
    color__{{ text }}
    {{ container_class }}
  ">
  {{ content }}
  <span 
    class="
      absolute rotate-180 bottom-full
      {% if tip == 'left' %}
        left-4
      {% elsif tip == 'center' %}
        left-auto right-auto
      {% elsif tip == 'right' %}
        right-4
      {% endif %}
    "
    style="
      border-width: 8px 8px 0 8px;
      {% if background == 'text' %}
        border-color: var(--color__background-secondary) transparent transparent transparent;
      {% else %}
        border-color: var(--color__background-body) transparent transparent transparent;
      {% endif %}
    ">
  </span>
</div>